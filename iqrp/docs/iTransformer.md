# iTransformer

Inverted Transformer: tokens are variates (features), attending across the feature axis after projecting the time dimension.

```python
model = create_transformer_model("itransformer")
```
